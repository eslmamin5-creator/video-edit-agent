# Adapter: generic agent

Read [`../core_instructions.md`](../core_instructions.md) first — everything
below is additive, not a replacement.

If your tool doesn't have a dedicated adapter file, this one applies. There
is nothing tool-specific to add: follow the core instructions, run
`pytest tests/ -v` before considering a change complete, and never bypass the
dialect-preservation rule or the ffmpeg argument-array rule.
